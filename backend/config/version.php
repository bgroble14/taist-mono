<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Application Version
    |--------------------------------------------------------------------------
    |
    | This is the current version of the application. This version is used
    | by the database migration to sync the version table, ensuring the
    | app version matches the database version.
    |
    | IMPORTANT: When updating the version in frontend/app.json, also update
    | this value to keep them in sync.
    |
    */

    'version' => env('APP_VERSION', '29.0.0'),
];

